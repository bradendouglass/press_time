%h1 List of Run Stamps

%table
  %thead
    %tr
      %th Id
      %th Job Number
      %th Time Start
      %th Time Stop
      %th Total Time
  %tbody
    -@run_stamps.each do |stamp|
      %tr
        %td= stamp.id
        %td= stamp.jobNumber
        %td= Time.at(stamp.timeStart).utc.strftime("%I:%M %p - %b %d, %Y")
        %td= Time.at(stamp.timeStop).utc.strftime("%I:%M %p - %b %d, %Y")

