%h1 List of Run Stamps

%table{ :rules => 'all' }
  %thead
    %tr
      %th Job Number
      %th Time Start
      %th Time Stop
      %th Total Time
  %tbody
    -@run_stamps.each do |stamp|
      %tr
        %td= stamp.jobNumber
        %td= time_at(stamp.timeStart)
        %td= time_at(stamp.timeStop)
        %td= Time.at(stamp.timeStop - stamp.timeStart).utc.strftime("(%H) hours (%M) min (%S) seconds")

